add_subdirectory(HttpClient)
if (NOT ${CMAKE_LIBRARY_ARCHITECTURE} MATCHES "arm64")
    add_subdirectory(SmartScopesClient)
else()
    message("Skipping SmartScopesClient test on arm64 - see LP #1615004")
endif()
add_subdirectory(smartscopesproxy)
add_subdirectory(SSConfig)
